enum AssetType {
  Organization = "organization",
  Site = "site",
  Phase = "phase",
  Department = "department",
  None = "none",
}

// the UI data to render the asset location data

export { AssetType };
